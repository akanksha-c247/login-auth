export interface ILoginAuth{
    email:String,
    password:any,
}

export interface FormData {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
  }

  export interface IAuth{
    email:String,
    authenticated:any,
}